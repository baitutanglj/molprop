from setuptools import setup, find_packages

setup(
    name="molprop",
    version="1.0",
    description="This is molprop. A package to predict the molecular property.",
    author="linjie",
    packages=find_packages(),
    include_package_data=True,
)